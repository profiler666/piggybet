import 'package:flutter/material.dart';

class JoinBetScreen extends StatelessWidget {
  const JoinBetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Join a PiggyBet'),
      ),
      body: const Center(
        child: Text('Join Bet Screen - Coming Soon'),
      ),
    );
  }
}